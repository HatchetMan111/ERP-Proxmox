# ERPNext Proxmox Installer

Dieses Repository enthält automatisierte Skripte, um **ERPNext v15** schnell und zuverlässig in einer **Proxmox VM** (Ubuntu 24.04) zu installieren. Das Design orientiert sich an den populären Proxmox Helper Scripts.

## 🚀 Schnellstart

Führe diesen Befehl in deiner **Proxmox Shell** aus, um eine vorkonfigurierte VM zu erstellen:

```bash
wget -qO- https://raw.githubusercontent.com/HatchetMan111/ERP-Proxmox/main/install.sh | bash
